class Pizza {
  dough = "";
  sauce = "";
  topping = "";

  setDough(dough) {
    this.dough = dough;
  }

  setSauce(sauce) {
    this.sauce = sauce;
  }

  setTopping(topping) {
    this.topping = topping;
  }
}

module.exports = Pizza;